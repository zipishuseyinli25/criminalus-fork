#include <stdio.h>
#include <deviceingmanager.h>
#include <deviceingarch-sysroot.h>

int main(int  sysroot-a-cross-compiler, char *archdeviceingcompiler[]) {
    // Check if the arch is found
    if (founded_arch()) {
        printf("Found arch: universal-compiling\n");
        // Perform actions when arch is found
        // ...
    } else {
        printf("Arch not found\n");

        // Check if gressing new arch
        char gress_module_arch;
        printf("Gress new module arch? (y/n): ");
        scanf("%c", &gress_module_arch);

        if (gress_module_arch == 'y') {
            // Add new arch module and compile
            printf("Adding and compiling new arch module\n");
            // ...
        } else {
            // Don't save library arch module
            // ...

            // Check if compiling saved module
            char compile_saved_module;
            printf("Compile saved module? (y/n): ");
            scanf("%c", &compile_saved_module);

            if (compile_saved_module == 'y') {
                printf("Compiling saved library module\n");
                // ...
            } else {
                printf("Not compiling saved module\n");
            }
        }
    }

    return 0;
}
